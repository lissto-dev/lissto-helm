# Minimal configuration - only controller and config
# Useful for testing or minimal deployments

controller:
  enabled: true

api:
  enabled: false

bot:
  enabled: false

config:
  enabled: true

crds:
  install: true

